import { useState } from 'react'
import { LockKeyhole, Trees } from 'lucide-react'
export default function Login({onDemo,onLogin,error}){const [email,setEmail]=useState('');const [password,setPassword]=useState('');return <main className="login"><section className="login-card"><div className="brand-mark"><Trees/></div><p className="eyebrow">QUẢN TRỊ XƯỞNG MỘC</p><h1>Nguyên liệu rõ ràng.<br/>Giá thành chắc chắn.</h1><p className="muted">Theo dõi lô gỗ, keo, sơn và phụ kiện xuyên suốt từng đơn hàng.</p>{error&&<div className="alert">{error}</div>}<form onSubmit={e=>{e.preventDefault();onLogin(email,password)}}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required placeholder="ban@xuongmoc.vn"/></label><label>Mật khẩu<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label><button className="primary" type="submit"><LockKeyhole size={17}/>Đăng nhập</button></form><button className="secondary wide" onClick={onDemo}>Vào bản demo local</button><small>Demo lưu trên trình duyệt, không gửi dữ liệu ra ngoài.</small></section></main>}
